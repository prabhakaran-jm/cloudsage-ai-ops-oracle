# For npm workspaces, we can't prevent hoisting directly
# But we can ensure Next.js is installed locally by running npm install in this directory
# The Netlify build should install dependencies locally when base directory is set

